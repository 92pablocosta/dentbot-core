[project]
name = "dentbot-core"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Pablo Costa", email = "92pablocosta@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
dentbot-core = "dentbot_core:main"

[build-system]
requires = ["uv_build>=0.11.7,<0.12.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
